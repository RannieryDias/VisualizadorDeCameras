namespace VisualizadorDeCameras
{
    public partial class ImageContainer : UserControl
    {
        public ImageContainer(string url, int id)
        {
            InitializeComponent(url, id);
        }
    }
}
